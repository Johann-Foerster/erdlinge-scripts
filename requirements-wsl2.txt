# WSL2-specific requirements file
# Install system packages first:
# sudo apt-get update
# sudo apt-get install python3-gi python3-gi-cairo gir1.2-webkit2-4.0
# sudo apt-get install python3-pyqt5 python3-pyqt5.qtwebkit
# export DISPLAY=:0  # or use WSLg on Windows 11

gradio>=4.0.0
tika
pandas
openpyxl
numpy
pyinstaller
pywebview
# GUI backend already installed via system packages
# No pip install needed for PyQt5 on WSL2